server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      routes:
        - id: demo-api
          uri: lb://DEMO-API
          predicates:
            - Path=/hello
