server:
  port: 8081

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111

message: "this is a demo api"

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
